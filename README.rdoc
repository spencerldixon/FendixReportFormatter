== Fendix Report Generator

This application takes a CSV file from Switch Concepts' adserver and uses it to create a nicely formatted report and geographical view of advertising locations. This can then be exported as a PDF and sent to agencies.

How to use:

* rake db:create db:migrate

* rake db:seed (Creates admin users from seed file)

* Import a CSV

* Download a PDF